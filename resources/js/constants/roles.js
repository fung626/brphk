import { i18n } from "../plugins";

const roles = [
    {
        name: `${i18n.t("admin")}`,
        value: "admin"
    },
    {
        name: `${i18n.t("employee")}`,
        value: "employee"
    }
];

export default roles;
